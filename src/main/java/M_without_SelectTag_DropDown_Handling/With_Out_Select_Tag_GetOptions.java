package M_without_SelectTag_DropDown_Handling;

public class With_Out_Select_Tag_GetOptions {

	public static void main(String[] args) {

		// Using driver.findElements();

		/*
		 * Any Drop Down we Can Handle It with This Below Code -Jquery - BootStrap -
		 * <Select > Tag Drop Down having Api even that Also we will Handle - with_out
		 * Select Tag Drop Down
		 */
		// In 3 Steps we can Handle Any->DropDown for Any-> web Application

	 //Step:(1) 
	// List<webElements> optionslistDay=driver.findElements(Xpath Parent SELECT -->Child Tag Options);
	//List<webElements> optionslistMonth=driver.findElements(Xpath Parent SELECT -->Child Tag Options);
	//List<webElements> optionslistYear=driver.findElements(Xpath Parent SELECT -->Child Tag Options);
	
		     
   //Step:(2) 
  //Generic Method
		
	
	     
  //Step:(3) 
 //Call the Generic Method
		
	}

}
